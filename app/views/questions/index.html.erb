<div class="flex flex-col">
  <h1 class="text-2xl mb-4">Questions</h1>

  <% @questions.each do |question| %>
<!--      <p class="mb-2"><%#= link_to "Вопрос #{question.id}", "/questions/#{question.id}" %></p>-->
      <p class="mb-2"><%= link_to "Вопрос #{question.id}",  question_path(question) %></p>
  <% end %>

  <%= link_to "New question", new_question_path %>
</div>
