<div>
  <div>
    <h1 class="mb-10 text-2xl">Question</h1>
    <p class="text-2xl mb-4"><%= @question.body %></p>
    <p>User_id: <%= @question.user_id %></p>
    <p class="mt-2"><%= link_to "Edit", edit_question_path %></p>
    <p class="mt-2"><%= link_to "Destroy", question_path(@question), data: { turbo_method: :delete, turbo_confirm: "Are you sure?" } %></p>
  </div>

  <div class="mt-8">
    <%= link_to "All questions", "/questions" %>
  </div>

</div>

